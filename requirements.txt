# Desktop application dependencies
tkinter  # Usually comes with Python, no install needed

# PDF generation (optional - for exporting results)
reportlab>=4.0.0

# For pyinstaller (optional - to create executable)
# pyinstaller>=5.0
